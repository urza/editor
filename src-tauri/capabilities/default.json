{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The page origin (GitHub Pages) gets the core defaults, clipboard write (Debug > Copy spike report), and the three window commands of architecture.md \u00a714.4 (open, focus, ready).",
  "remote": {
    "urls": [
      "https://urza.github.io/*"
    ]
  },
  "windows": [
    "main",
    "ws-*"
  ],
  "permissions": [
    "core:default",
    "clipboard-manager:allow-write-text",
    "allow-open-workspace",
    "allow-focus-workspace",
    "allow-page-ready"
  ]
}
