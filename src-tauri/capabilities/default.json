{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The page origin (GitHub Pages) gets the core defaults, clipboard write (Debug > Copy spike report), the window commands of architecture.md §14.4 (open, focus, close, ready), and the native disk backend of §17. The dialog plugin is deliberately absent: only Rust opens a picker, so a page script cannot.",
  "remote": {
    "urls": [
      "https://urza.github.io/*"
    ]
  },
  "windows": [
    "main",
    "ws-*"
  ],
  "permissions": [
    "core:default",
    "clipboard-manager:allow-write-text",
    "allow-open-workspace",
    "allow-focus-workspace",
    "allow-close-workspace",
    "allow-page-ready",
    "core:window:allow-set-title",
    "allow-disk-pick-folder",
    "allow-disk-pick-file",
    "allow-disk-pick-save",
    "allow-disk-list",
    "allow-disk-stat",
    "allow-disk-read",
    "allow-disk-read-bytes",
    "allow-disk-write",
    "allow-disk-rename",
    "allow-disk-prune"
  ]
}
